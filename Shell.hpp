
#include <string>
#include <iostream>


using namespace std;

namespace ex4
{
    // gets a string from terminal and processes it using the api specified for the program.
    void process_input(const string & str);
    // receives a string and returns the string followed by the first space.
    string getAfterSpace(const string & str);
    // raises error if invalid command entered.
    void raise_error(const string & str);

};









